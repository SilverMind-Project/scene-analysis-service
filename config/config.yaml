# scene-analysis-service default configuration
# All values can be overridden via environment variables prefixed with SAS_
# (e.g. SAS_TRITON_URL=localhost:8701)

# ── Triton Inference Server ──────────────────────────────────────────────────
# gRPC endpoint for Triton Inference Server.
# Leave empty to fall back to Null implementations (service starts but does no inference).
triton_url: ""

# ── Object detection (YOLO26L via Triton) ─────────────────────────────────────
yolo_enabled: true
yolo_model_name: "person-detector"
yolo_confidence_threshold: 0.25

# ── Scene description (Florence-2-large via Triton) ───────────────────────────
florence_enabled: true
florence_model_name: "florence-2"
florence_tokenizer_dir: "../continuous-tracking/triton-models/florence-2/1"
florence_task: "<DETAILED_CAPTION>"   # or <CAPTION>, <OD>, etc.

# ── Image embeddings (CLIP ViT-L/14 via Triton) ───────────────────────────────
clip_enabled: true
clip_model_name: "clip-vision"

# ── Hazard detection ──────────────────────────────────────────────────────────
hazards_config_path: "config/hazards.yaml"

# ── Service ───────────────────────────────────────────────────────────────────
host: "0.0.0.0"
port: 8300
log_level: "info"
max_image_size_px: 1920   # longest edge; larger images are downscaled before inference
